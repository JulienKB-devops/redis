name: Docker Image CI

on:
  #push:
    #branches: [ "main" ]
  #pull_request:
    #branches: [ "main" ]
  workflow_dispatch:

jobs:

  build:

    runs-on: 
      - self-hosted
      - runner-test

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: echo "salut" #docker build . --file Dockerfile --tag julien-KB:$(date +%s)
